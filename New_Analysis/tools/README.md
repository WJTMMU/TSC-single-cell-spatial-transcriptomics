# Tools

This directory stores reusable helper utilities that are not part of the maintained `01-21` manuscript pipeline.

Examples:

- custom enrichment plotting helpers
- interactive visualization helpers

These files may support figure refinement or ad hoc downstream exploration, but they should not be considered required steps for reproducing the core pipeline.
